framework项目简介：
    1、整体架构
        SpringBoot
            spring-boot-starter-parent2.0.3

        dubbo-spring-boot-starter0.2.0
            dubbo2.6.2、zookeeper3.4.9、

        pagehelper-spring-boot-starter1.2.7（mybatis分页jar包，其中包含mybatis依赖）
            mybatis3.4.6、 mybatis-spring1.3.2

        数据库MySQL
            mysql-connector-java5.1.46

        连接池
            druid-spring-boot-starter1.1.9


    2，功能组件
        注册中心
            zookeeper3.4.6

        分布式服务框架
            dubbo

        日志（还未添加）




    3，各模块说明
        portal是标准的dubbo服务消费者和服务提供者的2个应用；

        test是引入中台的概念，test-controller属于前台controller，接收转发请求；test-server属于中台层，就是业务逻辑层，是controller与dao的中间层，应用身份既是消费者也是提供者；
            test-dao是数据库访问层，是test-server的服务提供者；但是目前test-server和test-dao都无法注册zookeeper中；2019-04-14
            更新：test-server修改dubbo扫描包路径后成功注册到zookeeper中，既是消费者又是提供者，但是test-dao还是无法注册

            更新：2019-04-14 22:00 测试test-controller前台、test-server中台、调用portal-server后台可以实现一次完成的请求和响应；但是直接注入portal-provider中mybatis的mapper接口时注入失败；
                    dubbo能否注入一个没有实现类的接口？最终在test-dao服务加了一层service接口和实现类

            更新：will not be managed by Spring发现druid连接池创建连接未使用spring进行管理？？未解决，该问题是否没有使用druid连接池？有待验证